# Researcher Agent Rule Overlay

The researcher owns evidence gathering and fact chains. When using company or
project knowledge, cite entry ids, source filenames, or extracted text paths.

Images and documents marked `pending_extraction` may be used only as source
assets to inspect, not as already-proven facts.
