# Sanitized Profiles

These profiles contain SOUL documents and minimal no-secret `config.yaml` files.

Credentials must be supplied by environment variables, enterprise secret storage, or provider-specific configuration outside this repository.
